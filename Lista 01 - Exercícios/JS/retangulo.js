function retangulo(){
    let base = parseFloat(prompt("O valor da base:"));
    let altura = parseFloat(prompt("O valor da altura:"));
    let retangulo = base * altura;
    alert("A área de retângulo é: "+retangulo)
}

